packages:
  - 'apps/*'
  - 'packages/*'

# Native dependencies allowed to run install/build scripts.
# Replaces the deprecated package.json "pnpm.onlyBuiltDependencies" field.
allowBuilds:
  better-sqlite3: true
  esbuild: true
